spring:
  datasource:
    url: jdbc:postgresql://postgres-session-api:5432/session
    username: session
    password: session
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect